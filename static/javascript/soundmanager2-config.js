soundManager.setup({
    url: '/static/swf/',    // path to the SWF files
    flashVersion: 9,        // necessary for MP4 support
    preferFlash: false      // let's stick to HTML where possible
});
